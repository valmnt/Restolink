<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('index') }}">RestoLink</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a {{ app.request.attributes.get('_route') == 'index' ? 'class="nav-item nav-link active"' : 'class="nav-item nav-link"' }} href="{{ path('index') }}">Accueil</a>
      
        <a class="nav-item nav-link" href="{{ path('admin_restaurant_index') }}">Restaurants</a>
      {% if app.user %}
        <a class="nav-item nav-link" href="{{ path('admin_user_index') }}">Utilisateurs</a>
        <a class="nav-item nav-link" href="{{ path('restaurants_liste') }}">Retour au site</a>
        <a class="nav-item nav-link" href="{{ path('user', {'id': app.user.id}) }}">Mon Profil</a>
        <a class="nav-item nav-link" href="{{ path('app_logout') }}">Logout : {{ app.user.username }}</a>
     {% endif %}
        
      </div>
    </div>
</nav>
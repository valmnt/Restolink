{% extends 'base.html.twig' %}

{% block title %}Supprimer{% endblock %}

{% block body %}
<style>
#container {
    font-size: 140%;
}
.container_button {
    display: flex;
}
.button {
    margin-right: 1%;
}
</style>
<div style="display: flex; align-items: center; justify-content: center; width: 100%; height: 400px;">
    <div class="col-md-4" id="container">
        <h2>{{ user.nom }} {{ user.prenom }}</h2>
        <p>Souhaitez-vous vraiment supprimer voter compte ? <br/>
            Cette action est définitive et vous ne pourrez plus revenir en arrière ? 😭
        </p>
        <div class="container_button">
        <p class="button"><a class="btn btn-success" href="{{ path('user') }}" role="button">Annuler</a></p>
        <p><a class="btn btn-danger" role="button" href="{{ path('delete_user', {'id' : user.id })}}">Supprimer definitivement</a></p>
        </div>
    </div>
</div>
{% endblock %}